from setuptools import setup, find_packages

setup(name='emoint',
      version='0.0.1',
      description='Package used for working with emotion intensity dataset',
      author='Raj',
      author_email='brajnarayanan.b@gmail.com',
      packages=find_packages()
      )
